<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.ServerPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:Bloxstrap.Resources"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.Dialog_ServerPicker_Title}"
        MinWidth="0"
        MinHeight="0"
        Width="560"
        Height="480"
        ResizeMode="CanResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Bloxstrap.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" Title="{x:Static resources:Strings.Dialog_ServerPicker_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" Icon="pack://application:,,,/Bloxstrap.ico" />

        <DockPanel Grid.Row="1" Margin="16,8,16,8">
            <TextBlock DockPanel.Dock="Top" Text="{x:Static resources:Strings.Dialog_ServerPicker_Description}" TextWrapping="Wrap" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ListView x:Name="ServersList"
                      SelectionMode="Single" MouseDoubleClick="ServersList_MouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{x:Static resources:Strings.Dialog_ServerPicker_Column_Ping}" Width="72" DisplayMemberBinding="{Binding Ping, StringFormat={}{0:0}}" />
                        <GridViewColumn Header="{x:Static resources:Strings.Dialog_ServerPicker_Column_Players}" Width="90" DisplayMemberBinding="{Binding PlayersDisplay}" />
                        <GridViewColumn Header="{x:Static resources:Strings.Dialog_ServerPicker_Column_Id}" Width="320" DisplayMemberBinding="{Binding Id}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>

        <Border Grid.Row="2" Padding="15" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <DockPanel LastChildFill="False">
                <ui:Button DockPanel.Dock="Right" MinWidth="100" Appearance="Primary" Content="{x:Static resources:Strings.Dialog_ServerPicker_Join}" Click="Join_Click" IsDefault="True" />
                <ui:Button DockPanel.Dock="Right" Margin="0,0,12,0" MinWidth="100" Appearance="Secondary" Content="{x:Static resources:Strings.Common_Cancel}" Click="Cancel_Click" IsCancel="True" />
            </DockPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
