<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.LaunchRegionToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:Bloxstrap.Resources"
        mc:Ignorable="d"
        Title=""
        MinWidth="0"
        MinHeight="0"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="False"
        Background="Transparent"
        Loaded="Window_Loaded"
        Icon="pack://application:,,,/Bloxstrap.ico">
    <Border CornerRadius="8" Margin="12" Padding="16,12"
            Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.35" />
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="{x:Static resources:Strings.Dialog_LaunchRegion_Toast_Header}" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <TextBlock x:Name="BucketLineText" Margin="0,6,0,0" TextWrapping="Wrap" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock x:Name="DetailLineText" Margin="0,8,0,0" TextWrapping="Wrap" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </Border>
</base:WpfUiWindow>
